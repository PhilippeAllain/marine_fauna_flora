{% extends 'base.html.twig' %}

{% block title 'Glossaire' %}

{% block body %}
	<div class="container w-50 mt-4 p-3 border rounded shadow">
		<h1 class="text-center">Résultat de la recherche</h1>
		<div class="container text-center border border-5 border-primary my-5">
			<div class="row">
				<div class="col-4 align-self-center">

					{% if glossaries %}
						{% for glossary in glossaries%}
							<div class="container justify-content-center d-flex">
								<h3>
									<a class="text-primary" href="{{ path('glossary.show', {id: glossary.id}) }}">{{ glossary.word }}</a>
								</h3>
							</div>
						{% endfor %}
					{% elseif glossaries==[] %}

						<h2>Pas de résultat</h2>

					{% endif %}
				</div>

			</div>
			<div class="d-flex justify-content-center">{{ pagerfanta(glossaries) }}</div>
		</div>
		<div class="container justify-content-center d-flex">
			<a href="{{ path('glossary.index') }}" class="btn btn-primary text-center">Retour</a>
		</div>
	</div>
{% endblock %}
