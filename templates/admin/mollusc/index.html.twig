{% extends 'admin/admin.html.twig' %}

{% block title 'Les Mollusques' %}

{% block body %}
	<div class="container w-75 mt-4 p-3 border rounded shadow">
		<div class="container p-4">
			<h1 class="text-center">Les mollusques</h1>
		</div>

		<div class="mb-3 p-4 border rounded">
			{% include 'components/_search_data.html.twig' with {'form': form} only %}
		</div>

		<div class="container p-4">
			<p>
				<a class="btn btn-primary btn-sm" href="{{ path('admin.mollusc.create') }}">Ajouter un mollusque.</a>
			</p>
		</div>

		<div class="container p-4">
			<table class="table">
				<thead>
					<tr>
						<th>{{ knp_pagination_sortable(molluscs, 'Nom', 'm.name') }}</th>
						<th>{{ knp_pagination_sortable(molluscs, 'DCI', 'm.dci') }}</th>
						<th style="width: 217px">Actions</th>
					</tr>
				</thead>
				<tbody>
					{% for mollusc in molluscs %}
						<tr>
							<td>
								<a href="{{ path('admin.mollusc.show', {id: mollusc.id}) }}">{{ mollusc.name }}</a>
							</td>
							<td>
								<a href="{{ path('admin.mollusc.show', {id: mollusc.id}) }}">{{ mollusc.dci }}</a>
							</td>
							<td>
								<div class="d-flex gap-1">
									<a class="btn btn-primary btn-sm" href="{{ path('admin.mollusc.edit', {id : mollusc.id}) }}">Editer</a>
									<form action="{{ path('admin.mollusc.delete', {id: mollusc.id}) }}" method="POST">
										<input type="hidden" name="_method" value="DELETE">
										<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mollusc.id) }}">
										<button class="btn btn-danger btn-sm" type="submit">Supprimer</button>
									</form>
								</div>
							</td>
						</tr>
					</td>
				</tbody>
			</tr>
		{% endfor %}
	</tbody>
</table></div><div class="container p-4">
<div class="d-flex justify-content-center">{{ knp_pagination_render(molluscs) }}</div></div></div>{% endblock %}
