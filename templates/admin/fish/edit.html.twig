{% extends 'admin/admin.html.twig' %}

{% block title 'Poisson : ' ~ fish.name %}

{% block body %}
	<div class="container border rounded shadow">

		{% if fish is not null %}
			<h1 class="mb-4">Modifier le poisson : "{{ fish.name }}"</h1>
			<div class="card">
				<img src="{{ vich_uploader_asset(fish, 'thumbnailFile') }}" alt="{{ fish.name }}" class="img-fluid mb-4" style="max-width: 200px;">
				<div class="card-body">
					{{ form_start(form) }}
					{{ form_row(form.name) }}
					{{ form_row(form.dci) }}
					{{ form_row(form.content) }}
					{{ form_row(form.thumbnailFile) }}
					{{ form_row(form.url) }}
					{{ form_end(form) }}
				</div>
			</div>
		</div>
	{% else %}
		<h1 class="mb-4">Poisson non trouvé</h1>
		<p>Le poisson que vous essayez de modifier n'existe pas.</p>
	{% endif %}
</div>

<div class="container my-4 justify-content-center d-flex gap-3">
	<a href="{{ path('admin.fish.index') }}" class="btn btn-primary">Retour</a>
</div>{% endblock %}
