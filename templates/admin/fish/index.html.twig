{% extends 'admin/admin.html.twig' %}

{% block title 'Poissons' %}

{% block body %}
	<div class="container w-75 mt-4 p-3 border rounded shadow">
		<div class="container p-4">
			<h1 class="text-center">Poissons</h1>
		</div>

		<div class="mb-3 p-4 border rounded">
			{% include 'components/_search_data.html.twig' with {'form': form} only %}
		</div>

		<div class="container p-4">
			<p>
				<a class="btn btn-primary btn-sm" href="{{ path('admin.fish.create') }}">Ajouter un poisson.</a>
			</p>
		</div>

		<div class="container p-4">
			<table class="table">
				<thead>
					<tr>
						<th>{{ knp_pagination_sortable(fishes, 'Nom', 'f.name') }}</th>
						<th>{{ knp_pagination_sortable(fishes, 'DCI', 'f.dci') }}</th>
						<th style="width: 217px">Actions</th>
					</tr>
				</thead>
				<tbody>
					{% for fish in fishes %}
						<tr>
							<td>
								<a href="{{ path('admin.fish.show', {id: fish.id}) }}">{{ fish.name }}</a>
							</td>
							<td>
								<a href="{{ path('admin.fish.show', {id: fish.id}) }}">{{ fish.dci }}</a>
							</td>
							<td>
								<div class="d-flex gap-1">
									<a class="btn btn-primary btn-sm" href="{{ path('admin.fish.edit', {id : fish.id}) }}">Editer</a>
									<form action="{{ path('admin.fish.delete', {id: fish.id}) }}" method="POST">
										<input type="hidden" name="_method" value="DELETE">
										<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fish.id) }}">
										<button class="btn btn-danger btn-sm" type="submit">Supprimer</button>
									</form>
								</div>
							</td>
						</tr>
					</td>
				</tbody>
			</tr>
		{% endfor %}
	</tbody>
</table></div><div class="container p-4">
<div class="d-flex justify-content-center">{{ knp_pagination_render(fishes) }}</div></div></div>{% endblock %}
