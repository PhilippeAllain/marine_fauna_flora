{% extends 'base.html.twig' %}

{% block title 'Glossaire : ' ~ mammal.name %}

{% block body %}
	<div class="container w-50 mt-4 p-3 border rounded shadow">

		{% if mammal is not null %}
			<h1 class="mb-4 text-center">Détails du terme : "{{ mammal.name }}"</h1>
			<div class="card">
				<img src="{{ vich_uploader_asset(mammal, 'thumbnailFile') }}" alt="{{ mammal.name }}" class="card-img-top img-fluid mb-4 p-4" style="max-width: 200px;">
				<div class="card-body">
					<h2 class="card-header">{{ mammal.name }}</h2>
					<h3 class="card-title m-3">{{ mammal.description | nl2br }}</h3>
					<h4 class="card-text m-3">
						<strong>Exemples :</strong>
						{{ mammal.example }}</h4>
					<p class="card-text m-3 text-center">
						<a href="{{ mammal.url }}" target="_blank" class="btn btn-info mb-3">Voir la source</a>
					</p>
				</div>
			</div>
		</div>
	{% endif %}

	<div class="container my-4 justify-content-center d-flex gap-3">
		<a href="{{ path('mammal.index') }}" class="btn btn-primary">Retour</a>
	</div>
{% endblock %}
