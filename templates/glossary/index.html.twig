{% extends 'base.html.twig' %}

{% block title 'Glossaire' %}

{% block body %}
	<div class="container w-50 mt-4 p-3 border rounded shadow">
		<h1 class="text-center">Glossaire</h1>
		<div class="mb-3 p-4 border rounded">
			{% include 'components/_search_data.html.twig' with {'form': form} only %}
		</div>
		<div class="mb-3 p-4 border rounded">

			<table class="table table-primary table-striped-columns">
				<thead>
					<tr>
						<th>Nom</th>
					</tr>
				</thead>
				<tbody>
					{% for glossary in glossaries %}
						<tr>
							<td>
								<a href="{{ path('glossary.show', {id: glossary.id}) }}">{{ glossary.word }}</a>
							</td>
						</tr>
					</td>
				</tbody>
			</tr>
		{% endfor %}
	</tbody>
</table></div><div class="d-flex justify-content-center">{{ pagerfanta(glossaries) }}</div></div>{% endblock %}
